seprate words by comma

save more lists, up to 5
    syntax could be: list + nameOfList

share lists via whatsapp/email

opportunity to send a message to developer for debugging
    view: with email, name and motivation text

delete every space behind category name when creating new category

show the user, that all groceries in a category are checked. in a way of changing the arrow color or sth.

spinner design

marketing video