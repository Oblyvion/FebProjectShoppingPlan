addNewListButton
save more lists, up to 5
    syntax could be; list + nameOfList

view createNewList with edittext "List Name" & (List Symbol)
    edittext should write content into toolbar text directly
    back button

share lists via whattsapp/email
    compare all lists before sharing one, due to overriding effects

opportunity to send a message to developer for debuging
    view with email, name and motivation text



marketing video